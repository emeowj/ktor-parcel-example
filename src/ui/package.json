{
  "name": "ui",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "parcel watch templates/*.html --out-dir ../../resources/build --public-url /static",
    "build": "(rm -r dist || true) && parcel build templates/*.html --public-url /static --no-source-maps"
  },
  "devDependencies": {
    "autoprefixer": "^9",
    "parcel-bundler": "^1.12.4",
    "postcss": "^7",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "tailwindcss": "npm:@tailwindcss/postcss7-compat"
  }
}
